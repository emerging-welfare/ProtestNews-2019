Requirements

lxml : conda install -c anaconda lxml

pandas : conda install -c anaconda pandas

selenium : conda install -c conda-forge selenium

scrapy : conda install -c conda-forge scrapy

fuzzywuzzy : conda install -c conda-forge fuzzywuzzy

boilerpipe : sikintili biraz  == https://github.com/misja/python-boilerpipe

goose : https://pypi.org/project/goose-extractor/ ??

urlparse : ??


Hatalar:

Bazi haberler yok neden sadece download edebildiklerimizi yapmiyoruz??

ValueError: Unmatched ''"' when when decoding 'string'
